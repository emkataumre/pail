{
  "name": "pail",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": { "pail": "run.ts" },
  "scripts": {
    "start": "tsx run.ts",
    "promote": "tsx promote.ts",
    "drain": "tsx drain.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "check": "tsc --noEmit && vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^3.2.0"
  }
}
